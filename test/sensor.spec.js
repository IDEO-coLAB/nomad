const expect = require('chai').expect

describe('Sensor:', () => {
  before(() => {})
  after(() => {})

  describe('something:', () => {
    it('food', () => {
      expect(1).to.eql(1)
    })
  })
})
